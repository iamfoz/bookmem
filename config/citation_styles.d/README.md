# Custom citation styles

Add extra YAML files in this directory to define local citation styles.
Files are loaded after `config/citation_styles.yaml`, so a style with the same
key will override the built-in definition.

Keep local changes in separate files so upstream updates to the bundled styles
are easy to merge.
