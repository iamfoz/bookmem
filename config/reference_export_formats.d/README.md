# Local reference export formats

Add local BookMem reference export format definitions here as `.yaml` files.

Files in this directory are loaded after `config/reference_export_formats.yaml`, so a local file can add new formats or override a built-in format by using the same format key.
