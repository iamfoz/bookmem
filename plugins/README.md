# BookMem Plugins

BookMem supports lightweight plugin discovery through YAML manifests.

Plugin categories:

```text
importers/
enrichers/
exporters/
summary_providers/
citation_styles/
```

Each plugin can provide a `plugin.yaml` file.

See `docs/PLUGINS.md`.
